import { calculatePreferences2 } from "./calculatePreferences2.js";

// Example usage:
const numOnSide = 5;
const preferences = calculatePreferences2(numOnSide);

// Print preferences (you might want to format this better)
console.log(preferences); 
